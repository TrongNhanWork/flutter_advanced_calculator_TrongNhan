class ExpressionParser {
  // Utility class for parsing and validating expressions
  static bool isValid(String expression) {
    if (expression.isEmpty) return false;
    // Basic validation logic
    return true;
  }
}
